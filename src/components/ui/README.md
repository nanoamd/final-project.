# components/ui

shadcn/ui primitives (Button, Input, Dialog, ...). Generated by the shadcn CLI.

Rules:
- App-agnostic and low-level. Never import from `features/`, `server/`, or `config/`.
- May import only from `lib/` (e.g. the `cn` helper).
- Prefer customizing tokens/variants over forking a primitive.
