# components/shared

Composite components reused across more than one feature (e.g. `PageHeader`,
`EmptyState`, `DataTable`). If a component is used by a single feature, it belongs
in that feature instead.
